# MiMo TTS Android 编译说明

## 方法一：使用 Android Studio（推荐）

### 1. 环境要求
- Android Studio Ladybug (2024.2.1) 或更新版本
- JDK 17 或更高版本
- Android SDK 35

### 2. 编译步骤

```bash
# 1. 解压项目
unzip MiMoTTS-Android.zip
cd MiMoTTS-Android

# 2. 打开 Android Studio
# File -> Open -> 选择 MiMoTTS-Android 文件夹

# 3. 等待 Gradle 同步完成
# 首次同步会下载依赖，需要几分钟

# 4. 连接手机或启动模拟器

# 5. 点击 Run 按钮 (▶️) 编译并安装
```

### 3. 生成 APK

在 Android Studio 中：
- **Build → Build Bundle(s) / APK(s) → Build APK(s)**
- APK 将生成在 `app/build/outputs/apk/debug/app-debug.apk`

## 方法二：命令行编译

### 1. 安装 Android SDK

```bash
# 下载 Android Studio 命令行工具
wget https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
unzip commandlinetools-linux-11076708_latest.zip
mkdir -p /opt/android-sdk/cmdline-tools/latest
mv cmdline-tools/* /opt/android-sdk/cmdline-tools/latest/

# 设置环境变量
export ANDROID_HOME=/opt/android-sdk
export PATH=$PATH:$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools

# 安装 SDK 组件
sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0"
```

### 2. 编译项目

```bash
cd MiMoTTS-Android

# Linux/Mac
./gradlew assembleDebug

# Windows
gradlew.bat assembleDebug
```

### 3. 获取 APK

编译完成后，APK 文件位于：
```
app/build/outputs/apk/debug/app-debug.apk
```

## 方法三：使用 GitHub Actions 自动编译

你可以将项目推送到 GitHub，使用 GitHub Actions 自动编译 APK：

1. 创建 `.github/workflows/build.yml` 文件
2. 推送代码到 GitHub
3. 在 Actions 页面下载编译好的 APK

示例 workflow 文件已包含在项目中。

## 安装到手机

### 通过 ADB
```bash
adb install app/build/outputs/apk/debug/app-debug.apk
```

### 直接传输
1. 将 APK 文件传输到手机
2. 在文件管理器中点击安装
3. 允许"未知来源"安装

## 常见问题

### 1. Gradle 同步失败
- 检查网络连接
- 尝试更换 Maven 仓库镜像

### 2. 编译错误
- 确保 JDK 版本为 17+
- 清理项目：Build → Clean Project

### 3. 安装失败
- 开启开发者选项和 USB 调试
- 允许安装未知来源应用
